describe('Instance Helpers', function () {
  var mapInstance;
  // var result;

  beforeEach(function() {
    mapInstance = mapInstance || new GMaps({
      el : '#basic-map',
      lat: -12.0433,
      lng: -77.0283,
      zoom: 12
    });
  });


  describe('geocode', function() {

  });
});